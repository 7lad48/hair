import React from 'react';
import {Form} from "@/components/Contacts/Form/Form";

export const Contacts = () => {
    return (
        <div>
            <p>Contacts</p>
                <Form/>
        </div>
    );
}